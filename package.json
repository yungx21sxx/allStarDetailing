{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare",
    "yarn": "yarn add sass",
    "preview": "npx serve -s .output/public"
  },
  "devDependencies": {
    "@nuxt/image": "^0.7.1",
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.6",
    "sass": "^1.56.1",
    "yarn": "^1.22.19"
  }
}
